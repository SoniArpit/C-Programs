#include <stdio.h>
int main()
{
    printf("%lu \n", sizeof(int));
    printf("%lu \n", sizeof(char));
    printf("%lu \n", sizeof(float));
    printf("%lu \n", sizeof(double));
    return 0;
}

// size of data type is depend on architecture of machine